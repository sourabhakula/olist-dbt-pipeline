name: 'olist_pipeline'
version: '1.0.0'
profile: 'olist_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_pipeline:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
```

Same functionality, no clutter. Save it, then push:
```
git add dbt_project.yml
git commit -m "Clean up dbt_project.yml"
git push
